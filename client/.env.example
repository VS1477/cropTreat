# Local development can keep this as /api because Vite proxies /api to the Node server.
# On Vercel, set this to your Render Node server URL, for example:
# VITE_API_BASE=https://crop-treat-api.onrender.com
VITE_API_BASE=/api
